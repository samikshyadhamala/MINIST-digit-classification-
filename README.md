Built a from-scratch neural network framework in Python (NumPy) using OOP principles, implementing dense layers, ReLU/Sigmoid activations, binary cross-entropy loss, and backpropagation
with mini-batch training via a custom DataLoader; evaluated performance on a real-world binary
classification task.
